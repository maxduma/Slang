import React from 'react';

import spinner from '../../../assets/Spinner.gif';

const Spinner = () => {
    return (
        <div>
            <img src={spinner} />
        </div>
    )
}

export default Spinner;


